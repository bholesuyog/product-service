package com.app.messaging;

import lombok.experimental.UtilityClass;

@UtilityClass
public class TracerExtension {

//	public String curreentTraceId(final Tracer tracer) {
//		return ofNullable(tracer.currentSpan()).map(Span :: context).map(TraceContext :: traceId).orElse("");
//	}
}
